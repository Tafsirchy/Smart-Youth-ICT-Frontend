import PageSkeleton from '@/components/ui/PageSkeleton';

/**
 * Marketing-Specific Loading Segment
 * ─────────────────────────────────
 * Preserves the main navigation header and footer (if in layout) while 
 * content streams in.
 */
export default function MarketingLoading() {
  return <PageSkeleton />;
}
